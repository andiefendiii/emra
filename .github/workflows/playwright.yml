name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispacth: # jalan manual
  
jobs:
  andi:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - name: Install Playwright Browsers
      run: echo andi
